# คู่มือการติดตั้งและใช้งาน CIA Exam Prep Web App

โปรเจกต์นี้ได้รับการพัฒนาขึ้นเพื่อช่วยให้คุณเตรียมสอบ Certified Internal Auditor (CIA) ได้อย่างมีประสิทธิภาพสูงสุด โดยมีหน้าจอที่ใช้งานง่าย รองรับการทำข้อสอบทั้งภาษาไทยและอังกฤษแบบสองภาษา (Dual-Language) พร้อมระบบคำนวณคะแนน สถิติการใช้งาน และจำลองเวลาสอบจริง

---

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

```text
CIA-Exam-Prep/
├── index.html                 # โครงสร้างหน้าเว็บหลักของแอปพลิเคชัน
├── package.json               # รายการ Dependencies และ Scripts ของโปรเจกต์
├── vite.config.js             # ตั้งค่า Vite Development Server (Port: 3000)
├── SETUP_GUIDE.md             # คู่มือฉบับนี้
├── public/
│   └── data/
│       ├── PLACE_PDF_HERE.txt # ไฟล์แนะนำสำหรับวาง PDF ข้อสอบ
│       └── mock_questions.json# ข้อสอบจำลอง 5 ข้อ สำหรับทดสอบระบบทันที
├── src/
│   ├── main.js                # โค้ดควบคุมตรรกะระบบทำข้อสอบ (Timer, Stats, Scoring)
│   └── style.css              # สไตล์ดีไซน์ระดับ Premium (Dark Mode, Glassmorphism)
└── scripts/
    └── extract_questions.py   # สคริปต์สกัดข้อสอบจาก PDF และแปลภาษาด้วย Gemini API
```

---

## 🚀 วิธีการรันแอปพลิเคชัน (Running the Web App)

คุณสามารถเปิดใช้งานระบบทดสอบเพื่อลองใช้งานข้อสอบจำลอง (Mock Questions) ได้ทันทีโดยการทำตามขั้นตอนเหล่านี้:

1. **เปิดโฟลเดอร์นี้บน Terminal**
   ```bash
   cd /Users/popular/Desktop/CIA-Exam-Prep
   ```
2. **รัน Development Server** (ไม่ต้องรัน npm install แล้ว เนื่องจากผมได้ติดตั้ง Vite ให้คุณเรียบร้อยแล้ว)
   ```bash
   npm run dev
   ```
3. **เปิดเว็บเบราว์เซอร์** ไปที่ลิงก์ที่ Vite แสดงขึ้นมา (ปกติคือ [http://localhost:3000](http://localhost:3000)) เพื่อลองเล่นระบบทำข้อสอบได้ทันที!

---

## 📝 วิธีการสกัดข้อสอบและแปลภาษาจาก PDF ของคุณ (Extracting Your PDF Exam)

เมื่อคุณพร้อมที่จะนำข้อสอบจริงของคุณมาเข้าสู่ระบบ ให้ทำตามขั้นตอนดังนี้ครับ:

### ขั้นตอนที่ 1: วางไฟล์ PDF
ให้นำไฟล์ข้อสอบ CIA (ภาษาอังกฤษ) ที่เป็นไฟล์ PDF ไปวางไว้ในโฟลเดอร์นี้:
👉 [public/data/](file:///Users/popular/Desktop/CIA-Exam-Prep/public/data/)

### ขั้นตอนที่ 2: ติดตั้ง Library สำหรับสคริปต์ Python
เปิด Terminal ของคุณและติดตั้ง Library ต่อไปนี้:
```bash
pip install pypdf google-generativeai
```

### ขั้นตอนที่ 3: กำหนด Gemini API Key ของคุณ
ตั้งค่า API Key เพื่อส่งข้อสอบไปให้ Gemini ช่วยสกัด แปลภาษา และจัดฟอร์แมต:
```bash
export GEMINI_API_KEY="ใส่_API_KEY_ของคุณที่นี่"
```
*(หากไม่ได้รันคำสั่งนี้ ตอนรันสคริปต์ระบบจะมีช่องให้คุณกรอก API Key ใน Terminal เองครับ)*

### ขั้นตอนที่ 4: รันสคริปต์สกัดข้อสอบ
รันคำสั่งนี้เพื่อเริ่มกระบวนการสกัดข้อสอบ:
```bash
python scripts/extract_questions.py
```
สคริปต์จะอ่านข้อสอบจาก PDF แปลเป็นภาษาไทย และแปลงให้เป็นไฟล์ข้อสอบจริงที่ชื่อ `public/data/questions.json` ทันที เมื่อเสร็จสิ้นตัวแอปพลิเคชันจะโหลดคำถามใหม่นี้ไปใช้งานโดยอัตโนมัติครับ!

---

💡 *เคล็ดลับ: เพื่อผลลัพธ์ที่ดีที่สุด ตรวจสอบให้มั่นใจว่าไฟล์ PDF มีโครงสร้างข้อความที่สามารถอ่านได้ และเมื่อข้อสอบเข้าสู่ระบบเรียบร้อย คุณจะสามารถเลือกทำข้อสอบได้ตั้งแต่ 10 ถึง 125 ข้อ หรือใช้โหมดจำลองสอบจริง (125 ข้อ, 150 นาที) ได้ทันทีครับ!*
